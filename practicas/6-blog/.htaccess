<IfModule mod_rewrite.c>

	# Activar RewriteEngine
	RewriteEngine on

	# Reescribir la URL solicitada por el usuario
  # Rewriterule ^single/(.+)/(.+) single.php?id =$1&title=$2
  Rewriterule ^single/(.+)/(.+) single.php?id=$4&$url


</IfModule>
